##Workflow: Scripts

#1. With Control F and query, you can select all and edit each instance of a
#word. Which is great for changing object names.

#2. Check arguments to R function calls, Warn if variable used has no definition
#in scope, Warn if variable is defined but not used, Provide R style diagnostics
#(e.g. whitespace)